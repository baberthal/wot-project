{
  "name": "@wot/clock",
  "version": "0.1.0",
  "author": "J. Morgan Lieberthal <j.morgan.lieberthal@gmail.com>",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "MIT",
  "scripts": {
    "configure": "node-gyp configure",
    "build": "yarn build:native && yarn build:ts",
    "build:native": "node-gyp build",
    "build:ts": "tsc",
    "rebuild": "yarn rebuild:native && yarn rebuild:ts",
    "rebuild:native": "node-gyp rebuild",
    "rebuild:ts": "yarn clean:ts && yarn build:ts",
    "clean": "yarn clean:native && yarn clean:ts",
    "clean:native": "rimraf build",
    "clean:ts": "rimraf dist",
    "pretest": "yarn rebuild",
    "test": "jest"
  },
  "os": [
    "linux",
    "freebsd",
    "darwin"
  ],
  "dependencies": {
    "bindings": "^1.4.0",
    "nan": "^2.12.1"
  },
  "devDependencies": {
    "@types/bindings": "^1.3.0",
    "@types/node": "^11.9.4",
    "jest": "^24.1.0",
    "node-gyp": "^3.8.0",
    "rimraf": "^2.6.3",
    "tslint": "^5.12.1",
    "typescript": "^3.3.3"
  }
}
